function setup() {
  createCanvas(200, 200);


  // Switch the color mode to RGB:
  colorMode(RGB);

  // Try different RGB values:
  //         +------------ red (0..255)
  //         |    +------- green (0..255)
  //         |    |  +---- blue (0..255)
  //         v    v  v
  background(255, 0, 0);
}
