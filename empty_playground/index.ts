const divRes - div(10, 2);
console.info(divRes);

// Wenn y = 0 -> 0 zurückgeben
function div(x: number, y: number): number {
    if (y === 0) {

    }

 const result = x / y;
 return result;
  }

  function print(message: string)